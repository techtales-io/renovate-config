{
  $schema: "https://docs.renovatebot.com/renovate-schema.json",
  description: "flux: automerge non major versions",
  packageRules: [
    {
      matchManagers: ["flux"],
      automerge: true,
      matchUpdateTypes: ["minor", "patch", "digest"],
      automergeType: "pr",
    },
  ],
}
