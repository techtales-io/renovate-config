{
  $schema: "https://docs.renovatebot.com/renovate-schema.json",
  description: "npm: automerge non major versions",
  packageRules: [
    {
      matchManagers: ["npm"],
      automerge: true,
      matchUpdateTypes: ["minor", "patch", "digest"],
      automergeType: "pr",
    },
  ],
}
